# Supabase configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-public-anon-key

# QR code signing secret (use a long, random string)
VITE_QR_SIGNING_SECRET=replace-with-long-random-secret

# Stripe (frontend keys only – keep secret key on your backend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
# Optional: surface your backend endpoint for the Stripe helper
VITE_API_URL=https://your-backend.example.com/api

# Email provider (used when resend/refund flows are wired up)
VITE_EMAIL_PROVIDER=sendgrid
VITE_EMAIL_API_KEY=replace-with-email-provider-key
VITE_EMAIL_FROM_ADDRESS=tickets@example.com

# URL configuration
VITE_FRONTEND_URL=https://your-frontend.example.com
# Optional: set when deploying under a sub-path (otherwise keep "/")
VITE_APP_BASE_PATH=/
