/**
 * VIP Table Service
 * Service layer for VIP table operations
 * 
 * This is a wrapper/alias for the main vip-tables-service in lib/
 * to match the expected file structure.
 */

// Re-export everything from the main VIP tables service
export * from '../lib/vip-tables-service';

// Note: vip-tables-service.ts only has named exports, no default export

